package com.learn.javacore._14FinalKeyword;

public class Test {

    //Final
    // constant -> member (cannot change), method (cannot override), class (cannot inherite)

    public static void main(String[] args) {
        Car car=new Car();
        System.out.println(car.getSpeed());
    }
}
